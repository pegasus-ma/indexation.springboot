version: "3.3"
services:
 nginx:
  image: nginx
  ports:
   - "8080:8080"
  links:
   - indexation
  volumes: 
   - "./nginx/conf.d/:/etc/nginx/conf.d"
 indexation:
  image: pegasusma/indexation:0-SNAPSHOT