1 - Ajouter ces balises dans le fichier settings.xml de maven

<pluginGroup>com.spotify</pluginGroup>
into
<pluginGroups>
</pluginGroups>

<!-- for pushing -->
<server>
  <id>docker-hub</id>
  <username>pegasusma</username>
  <password>hma@bucares</password>
  <configuration>
	  <email>hongyu.ma@hotmail.fr</email>
  </configuration>
</server>
into
<servers>
</servers>

2 - Package command

clean package + docker build + push : 
>mvn clean package docker:build -DpushImage

Aujourd'hui, pegasusma/indexation:0-SNAPSHOT

3 - Docker-compose

>docker-compose up